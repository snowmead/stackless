# Live smoke: provisions `laravel_cloud/application` through Stripe Projects
# under `--on local`. Run via:
#   mise run smoke-integration-laravel-cloud
# One-time: `stripe projects link laravel_cloud` (account-level).

[stack]
name = "smoke-laravel-cloud"

[stack.projects]
[stack.projects.stripe]
# project anchor recorded on first successful `up`

[integrations.app]
provider   = "laravel-cloud"
name       = "stackless-smoke-${instance.name}"
repository = "stackless-smoke"
region     = "us-east-1"

[services.web]
source      = { repo = "https://github.com/snowmead/stackless", ref = "main" }
root_origin = true
health      = { path = "/", contains = "stackless-smoke-ok" }
env         = { PROBE = "${integrations.app.app_id}" }

[services.web.local]
run = "sh -c 'd=$(mktemp -d); printf stackless-smoke-ok > \"$d/index.html\"; exec python3 -m http.server $PORT --bind 127.0.0.1 --directory \"$d\"'"
