# Live smoke: provisions `parallel/api` through Stripe Projects
# under `--on local`. Run via:
#   mise run smoke-integration-parallel
# One-time: `stripe projects link parallel` (account-level).

[stack]
name = "smoke-parallel"

[stack.projects]
[stack.projects.stripe]
# project anchor recorded on first successful `up`

[integrations.api]
provider = "parallel"

[services.web]
source      = { repo = "https://github.com/snowmead/stackless", ref = "main" }
root_origin = true
health      = { path = "/", contains = "stackless-smoke-ok" }
env         = { PROBE = "${integrations.api.api_key}" }

[services.web.local]
run = "sh -c 'd=$(mktemp -d); printf stackless-smoke-ok > \"$d/index.html\"; exec python3 -m http.server $PORT --bind 127.0.0.1 --directory \"$d\"'"
