# Live smoke: provisions `postalform/mail` through Stripe Projects
# under `--on local`. Run via:
#   mise run smoke-integration-postalform
# One-time: `stripe projects link postalform` (account-level).

[stack]
name = "smoke-postalform"

[stack.projects]
[stack.projects.stripe]
# project anchor recorded on first successful `up`

[integrations.mail]
provider = "postalform"

[services.web]
source      = { repo = "https://github.com/snowmead/stackless", ref = "main" }
root_origin = true
health      = { path = "/", contains = "stackless-smoke-ok" }
env         = { PROBE = "${integrations.mail.api_key}" }

[services.web.local]
run = "sh -c 'd=$(mktemp -d); printf stackless-smoke-ok > \"$d/index.html\"; exec python3 -m http.server $PORT --bind 127.0.0.1 --directory \"$d\"'"
