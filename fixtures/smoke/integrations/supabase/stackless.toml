# Live smoke: provisions `supabase/project` through Stripe Projects
# under `--on local`. Run via:
#   mise run smoke-integration-supabase
# One-time: `stripe projects link supabase` (account-level).

[stack]
name = "smoke-supabase"

[stack.projects]
[stack.projects.stripe]
# project anchor recorded on first successful `up`

[integrations.project]
provider = "supabase"

[services.web]
source      = { repo = "https://github.com/snowmead/stackless", ref = "main" }
root_origin = true
health      = { path = "/", contains = "stackless-smoke-ok" }
env         = { PROBE = "${integrations.project.url}" }

[services.web.local]
run = "sh -c 'd=$(mktemp -d); printf stackless-smoke-ok > \"$d/index.html\"; exec python3 -m http.server $PORT --bind 127.0.0.1 --directory \"$d\"'"
