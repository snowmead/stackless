# Live smoke: provisions `posthog/analytics` through Stripe Projects
# under `--on local`. Run via:
#   mise run smoke-integration-posthog
# One-time: `stripe projects link posthog` (account-level).

[stack]
name = "smoke-posthog"

[stack.projects]
[stack.projects.stripe]
# project anchor recorded on first successful `up`

[integrations.analytics]
provider = "posthog"

[services.web]
source      = { repo = "https://github.com/snowmead/stackless", ref = "main" }
root_origin = true
health      = { path = "/", contains = "stackless-smoke-ok" }
env         = { PROBE = "${integrations.analytics.api_key}" }

[services.web.local]
run = "sh -c 'd=$(mktemp -d); printf stackless-smoke-ok > \"$d/index.html\"; exec python3 -m http.server $PORT --bind 127.0.0.1 --directory \"$d\"'"
