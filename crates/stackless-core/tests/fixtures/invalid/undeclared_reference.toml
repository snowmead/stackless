[stack]
name = "bad"

[services.api]
source = { repo = "https://example.invalid/api", ref = "main" }
env    = { CLERK_SECRET_KEY = "${integrations.clerk.secret_key}" }
health = { path = "/health" }

[services.api.local]
run = "true"
