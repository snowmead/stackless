[stack]
name = "bad"

[secrets]
required = ["OTHER_KEY"]

[services.web]
source  = { repo = "https://example.invalid/web", ref = "main" }
secrets = ["CLERK_SECRET_KEY"]
health  = { path = "/" }

[services.web.local]
run = "true"
