# Generated by cargo-progenitor 0.14.0 from specs/vercel-trimmed.json.
# Do not hand-edit src/lib.rs — regenerate with `specs/regen-clients.sh`.
# This crate intentionally does NOT opt into [workspace.lints]: the generated
# client is not held to the workspace's unwrap/clippy/missing_debug rules.
[package]
name                 = "vercel-client"
edition.workspace    = true
version.workspace    = true
license.workspace    = true
repository.workspace = true

[dependencies]
bytes = "1.9"
chrono = { version = "0.4", default-features = false, features = ["serde"] }
futures-core = "0.3"
progenitor-client = "0.14.0"
regress = "0.10"
reqwest = { version = "0.13.4", default-features = false, features = [
    "rustls",
    "charset",
    "http2",
    "json",
    "stream",
] }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
serde_urlencoded = "0.7"
