/target
**/*.env.local

.projects/cache
.projects/vault
.projects/state.test.json
.projects/state.local.test.json

# Per-stack Stripe Projects runtime state for smoke fixtures (project id is recorded in the toml)
fixtures/smoke/*/.projects/

.env
.env.*
!.env.example
.stackless.env
**/.stackless.env

# mise local overrides (per-developer tools/env)
.mise.local.toml
*.local.toml

# cargo-dist may write temporary plan/build state outside target/ in some modes
# (normally everything lands under target/dist or the dist dir configured by metadata)
.dist/

# Cursor /orchestrate run state (plan.json, state.json, handoffs — local to a planner session)
.orchestrate/

# Marketing site (web/)
node_modules/
dist/
.vercel/
