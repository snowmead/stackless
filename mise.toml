[tools]
rust                  = "1.96.0"
taplo                 = "latest"
"cargo:cargo-nextest" = "latest"
"cargo:cargo-audit"   = "latest"
"cargo:cargo-deny"    = "latest"
"cargo:cargo-vet"     = "latest"
"cargo:cargo-dist"    = "latest"
prek                  = "latest" # pre-commit runner; [hooks] below auto-wires it

[tasks]
# Formatting & lint (keep clippy/fmt wired even if not "crap" tools)
fmt    = "cargo fmt --all -- --check"
clippy = "cargo clippy --workspace --all-targets --all-features -- -D warnings"
taplo  = "taplo fmt --check --diff"
# Testing — `--all-features` matches clippy and runs the `test-support`-gated
# fixtures (e.g. stackless-git's `flow` test, which needs `build_repo`).
test = "cargo nextest run --workspace --all-features"
# "Cargo crap" / supply chain (the point of the request)
# cargo-audit doesn't read deny.toml, so mirror its [advisories].ignore here
# (libsql's pinned old rustls stack — rustls-webpki / rustls-pemfile).
audit        = "cargo audit --ignore RUSTSEC-2026-0049 --ignore RUSTSEC-2026-0098 --ignore RUSTSEC-2026-0099 --ignore RUSTSEC-2026-0104 --ignore RUSTSEC-2025-0134"
deny         = "cargo deny check"
vet          = "cargo vet"
supply-chain = { depends = ["audit", "deny", "vet"] }
# One-command local gate (mirrors what CI will do)
check = { depends = ["fmt", "clippy", "taplo"] }
ci    = { depends = ["check", "test", "supply-chain"] }

# Live cloud smokes — gated on real provider creds. On the canonical repo they
# run on every PR via ci.yml `live-smoke` jobs (and nightly via smoke.yml).
# Locally they read tokens from .stackless.env; CI injects STRIPE_API_KEY /
# VERCEL_TOKEN / RENDER_API_KEY / FLY_API_TOKEN as secrets. All share
# `fixtures/smoke/run.sh`: a real up, status, logs, down, with a unique per-run
# instance name; the task fails if up (health), JSON contract checks, or down
# (verified-gone) fails.
smoke-vercel = "bash fixtures/smoke/run.sh vercel fixtures/smoke/vercel/stackless.toml smoke-v"
smoke-render = "bash fixtures/smoke/run.sh render fixtures/smoke/render/stackless.toml smoke-r"
smoke-cloudflare = "bash fixtures/smoke/run-integrations.sh --vendor cloudflare"
smoke-fly = "bash fixtures/smoke/run.sh fly fixtures/smoke/flyio/stackless.toml smoke-f --confirm-paid"
smoke-netlify = "bash fixtures/smoke/run.sh netlify fixtures/smoke/netlify/stackless.toml smoke-n"
smoke = { depends = [
    "smoke-vercel",
    "smoke-render",
    "smoke-cloudflare",
    "smoke-fly",
    "smoke-netlify",
] }

# Hermetic agent JSON contract gate (every lifecycle verb on agent-demo).
agent-demo-lifecycle = "bash fixtures/agent-demo-lifecycle.sh"

# Fleet plane live verification (gated; needs STACKLESS_STATE_URL + TOKEN).

# BEGIN smoke-integration tasks (generated by scripts/generate_smoke_fixtures.py)
smoke-integration-agentmail              = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/agentmail/stackless.toml smoke-i-agentmail"
smoke-integration-agentphone             = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/agentphone/stackless.toml smoke-i-agentphone --confirm-paid"
smoke-integration-algolia                = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/algolia/stackless.toml smoke-i-algolia --confirm-paid"
smoke-integration-amplitude              = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/amplitude/stackless.toml smoke-i-amplitude"
smoke-integration-auth0                  = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/auth0/stackless.toml smoke-i-auth0"
smoke-integration-base44                 = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/base44/stackless.toml smoke-i-base44"
smoke-integration-blaxel-agent-drive     = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/blaxel-agent-drive/stackless.toml smoke-i-blaxel-agent-drive"
smoke-integration-blaxel-sandbox         = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/blaxel-sandbox/stackless.toml smoke-i-blaxel-sandbox"
smoke-integration-browserbase            = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/browserbase/stackless.toml smoke-i-browserbase"
smoke-integration-chroma                 = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/chroma/stackless.toml smoke-i-chroma --confirm-paid"
smoke-integration-clerk                  = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/clerk/stackless.toml smoke-i-clerk"
smoke-integration-clickhouse             = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/clickhouse/stackless.toml smoke-i-clickhouse"
smoke-integration-clickhouse-postgres    = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/clickhouse-postgres/stackless.toml smoke-i-clickhouse-postgres"
smoke-integration-cloudflare-browser-run = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/cloudflare-browser-run/stackless.toml smoke-i-cloudflare-browser-run"
smoke-integration-cloudflare-d1          = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/cloudflare-d1/stackless.toml smoke-i-cloudflare-d1"
smoke-integration-cloudflare-hyperdrive  = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/cloudflare-hyperdrive/stackless.toml smoke-i-cloudflare-hyperdrive"
smoke-integration-cloudflare-kv          = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/cloudflare-kv/stackless.toml smoke-i-cloudflare-kv"
smoke-integration-cloudflare-queues      = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/cloudflare-queues/stackless.toml smoke-i-cloudflare-queues"
smoke-integration-cloudflare-r2          = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/cloudflare-r2/stackless.toml smoke-i-cloudflare-r2 --confirm-paid"
smoke-integration-cloudflare-workers     = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/cloudflare-workers/stackless.toml smoke-i-cloudflare-workers"
smoke-integration-cloudflare-workers-ai  = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/cloudflare-workers-ai/stackless.toml smoke-i-cloudflare-workers-ai"
smoke-integration-daytona                = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/daytona/stackless.toml smoke-i-daytona"
smoke-integration-e2b                    = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/e2b/stackless.toml smoke-i-e2b"
smoke-integration-elevenlabs             = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/elevenlabs/stackless.toml smoke-i-elevenlabs"
smoke-integration-exa                    = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/exa/stackless.toml smoke-i-exa"
smoke-integration-firecrawl              = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/firecrawl/stackless.toml smoke-i-firecrawl"
smoke-integration-flyio-mpg              = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/flyio-mpg/stackless.toml smoke-i-flyio-mpg --confirm-paid"
smoke-integration-flyio-sprite           = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/flyio-sprite/stackless.toml smoke-i-flyio-sprite --confirm-paid"
smoke-integration-gitlab                 = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/gitlab/stackless.toml smoke-i-gitlab"
smoke-integration-heygen                 = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/heygen/stackless.toml smoke-i-heygen --confirm-paid"
smoke-integration-huggingface            = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/huggingface/stackless.toml smoke-i-huggingface"
smoke-integration-huggingface-bucket     = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/huggingface-bucket/stackless.toml smoke-i-huggingface-bucket"
smoke-integration-inngest                = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/inngest/stackless.toml smoke-i-inngest"
smoke-integration-kernel                 = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/kernel/stackless.toml smoke-i-kernel"
smoke-integration-laravel-cloud          = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/laravel-cloud/stackless.toml smoke-i-laravel-cloud"
smoke-integration-laravel-cloud-mysql    = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/laravel-cloud-mysql/stackless.toml smoke-i-laravel-cloud-mysql"
smoke-integration-laravel-cloud-valkey   = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/laravel-cloud-valkey/stackless.toml smoke-i-laravel-cloud-valkey"
smoke-integration-metronome              = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/metronome/stackless.toml smoke-i-metronome"
smoke-integration-mixpanel               = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/mixpanel/stackless.toml smoke-i-mixpanel"
smoke-integration-neon                   = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/neon/stackless.toml smoke-i-neon"
smoke-integration-openrouter             = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/openrouter/stackless.toml smoke-i-openrouter"
smoke-integration-parallel               = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/parallel/stackless.toml smoke-i-parallel --confirm-paid"
smoke-integration-planetscale-mysql      = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/planetscale-mysql/stackless.toml smoke-i-planetscale-mysql --confirm-paid"
smoke-integration-planetscale-postgresql = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/planetscale-postgresql/stackless.toml smoke-i-planetscale-postgresql --confirm-paid"
smoke-integration-postalform             = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/postalform/stackless.toml smoke-i-postalform --confirm-paid"
smoke-integration-posthog                = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/posthog/stackless.toml smoke-i-posthog"
smoke-integration-prisma                 = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/prisma/stackless.toml smoke-i-prisma"
smoke-integration-privy                  = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/privy/stackless.toml smoke-i-privy"
smoke-integration-railway-bucket         = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/railway-bucket/stackless.toml smoke-i-railway-bucket"
smoke-integration-railway-hosting        = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/railway-hosting/stackless.toml smoke-i-railway-hosting"
smoke-integration-railway-mongo          = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/railway-mongo/stackless.toml smoke-i-railway-mongo"
smoke-integration-railway-postgres       = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/railway-postgres/stackless.toml smoke-i-railway-postgres"
smoke-integration-railway-redis          = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/railway-redis/stackless.toml smoke-i-railway-redis"
smoke-integration-render-postgres        = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/render-postgres/stackless.toml smoke-i-render-postgres --confirm-paid"
smoke-integration-runloop                = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/runloop/stackless.toml smoke-i-runloop"
smoke-integration-sentry                 = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/sentry/stackless.toml smoke-i-sentry"
smoke-integration-sentry-seer            = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/sentry-seer/stackless.toml smoke-i-sentry-seer"
smoke-integration-supabase               = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/supabase/stackless.toml smoke-i-supabase"
smoke-integration-supermemory            = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/supermemory/stackless.toml smoke-i-supermemory --confirm-paid"
smoke-integration-turso                  = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/turso/stackless.toml smoke-i-turso"
smoke-integration-upstash-qstash         = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/upstash-qstash/stackless.toml smoke-i-upstash-qstash --confirm-paid"
smoke-integration-upstash-redis          = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/upstash-redis/stackless.toml smoke-i-upstash-redis --confirm-paid"
smoke-integration-upstash-search         = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/upstash-search/stackless.toml smoke-i-upstash-search --confirm-paid"
smoke-integration-upstash-vector         = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/upstash-vector/stackless.toml smoke-i-upstash-vector --confirm-paid"
smoke-integration-wix                    = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/wix/stackless.toml smoke-i-wix --confirm-paid"
smoke-integration-wordpress-com          = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/wordpress-com/stackless.toml smoke-i-wordpress-com --confirm-paid"
smoke-integration-workos                 = "bash fixtures/smoke/run.sh local fixtures/smoke/integrations/workos/stackless.toml smoke-i-workos --confirm-paid"
smoke-integrations                       = "bash fixtures/smoke/run-integrations.sh --from-manifest"
smoke-integrations-vendor                = "bash fixtures/smoke/run-integrations.sh"
smoke-integrations-audit                 = "bash fixtures/smoke/stripe-link-audit.sh"
# END smoke-integration tasks
smoke-fleet = '''
[ -f .stackless.env ] && { set -a; . ./.stackless.env; set +a; }
if [ -z "${STACKLESS_STATE_URL:-}" ] || [ -z "${STACKLESS_STATE_TOKEN:-}" ]; then
  if [ "${STACKLESS_LIVE_SMOKE_REQUIRED:-}" = "1" ]; then
    echo "::error::STACKLESS_STATE_URL and STACKLESS_STATE_TOKEN required for live fleet smoke"
    exit 1
  fi
  echo "skip smoke-fleet: set STACKLESS_STATE_URL and STACKLESS_STATE_TOKEN in .stackless.env"
  exit 0
fi
cargo nextest run -p stackless-core --all-features -E 'binary(turso_fleet_lock)' --run-ignored only --no-capture
'''

# Stripe Projects plugin snapshots. `stripe-refresh` is the bless path (writes
# the committed fixtures from the locally installed plugin); it needs the real
# `stripe` CLI + creds, so it is EXCLUDED from `ci`/`check`/`test` and is invoked
# by the CI watcher, not by hand. `stripe-coherence` is the fast, offline
# pre-commit check (it also runs inside `mise run test`).
stripe-refresh = '''
[ -f .stackless.env ] && { set -a; . ./.stackless.env; set +a; }
STRIPE_PROJECTS_REFRESH=1 cargo nextest run -p stackless-stripe-projects --all-features -E 'test(refresh_blesses_snapshots)' --no-capture
'''
stripe-coherence = "cargo nextest run -p stackless-stripe-projects -E 'test(fixtures_are_coherent)'"
# Regenerate the vendored OpenAPI client crates (body stays shell — python +
# cargo-progenitor glue with no Rust types to reuse).
regen-clients           = "bash specs/regen-clients.sh"
smoke-rollout-wave      = "bash scripts/smoke_rollout_wave.sh"
generate-smoke-fixtures = "python3 scripts/generate_smoke_fixtures.py"

# Provider-onboarding tooling (the `xtask` crate). `catalog` is offline (reads the
# committed catalog fixture); `discover` is live — it provisions a resource into a
# throwaway environment to capture its real credential envelope, so it needs creds
# (STRIPE_API_KEY) and a dir with a linked project (default cwd; pass `-- --dir ...`).
# e.g. `mise run catalog cloudflare`, `mise run discover cloudflare/kv -- --dir fixtures/smoke/cloudflare`
catalog = "cargo run -q -p xtask -- catalog"
new-integration = "cargo run -q -p xtask -- new-integration"
discover = '''
[ -f .stackless.env ] && { set -a; . ./.stackless.env; set +a; }
cargo run -q -p xtask -- discover "$@"
'''

# Install the prek git hooks by hand (normally auto-wired by [hooks].postinstall).
hooks = "prek install -t pre-commit -t pre-push -f"

# Auto-wire the prek git hooks after `mise install` — the bootstrap devs and CI
# already run — so the pre-commit/pre-push gates need no separate setup step.
# Best-effort: a hiccup here must never fail `mise install`.
[hooks]
postinstall = "prek install -t pre-commit -t pre-push -f || true"

[settings]
# Required for [hooks] (auto-installing the prek git hooks above).
experimental = true
